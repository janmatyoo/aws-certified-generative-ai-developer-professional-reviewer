OpenSearch
- vector database and applies topic-based segmentation by isolating content into per-topic indices
- This design provides both efficient metadata management and the rich contextual information that you need for FM interactions
- This design provides the fastest query response to support advanced filtering and semantic search across millions of documents.
